{
  "mcpServers": {
    "unpaid-ra": {
      "command": "uv",
      "args": ["run", "python", "mcp/server.py"],
      "env": {
        "SEMANTIC_SCHOLAR_KEY": "${SEMANTIC_SCHOLAR_KEY}"
      }
    }
  }
}
